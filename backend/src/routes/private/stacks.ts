// imports
    // express
    import { Router } from "express";
    
    // middlewares

    // controllers

// router config
    // definition
    const stacksRouter = Router();
    
    // registering routes
    // stacksRouter.get('/', middleware1, middleware2, controller);

// exporting
export default stacksRouter;